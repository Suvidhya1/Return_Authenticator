# Return_Authenticator
When a customer requests a return, SmartReturn runs it through an ML model that checks return history, product type, time-since-purchase, and stated reason — then auto-approves, flags, or denies it. Built with MERN + a Flask ML microservice. Cuts fraud, speeds up legit returns.
